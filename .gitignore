node_modules/
dist/
responses/
*.log
.DS_Store
*.tsbuildinfo

# Local agent bootstrap — keep on disk, never publish
CHAT_RESTORE.md

# Drop-zone for other-agent patches/tests — not part of the repo
patch/
